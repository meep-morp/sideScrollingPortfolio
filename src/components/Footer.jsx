import React from "react";

const Footer = props => {
    return (
        <footer>
            <a href="#">Work</a>
            <a href="#">About</a>
            <a href="#">Contact</a>
        </footer>
    )
}

export default Footer;